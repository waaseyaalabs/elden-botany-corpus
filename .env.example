# Kaggle API credentials
# Obtain from https://www.kaggle.com/settings/account → Create New API Token
KAGGLE_USERNAME=
KAGGLE_KEY=

# PostgreSQL connection
POSTGRES_DSN=postgresql://user:pass@localhost:5432/elden

# Embedding provider configuration
EMBED_PROVIDER=openai  # 'openai' or 'local'
OPENAI_API_KEY=

# Optional: for local embeddings (sentence-transformers)
# EMBED_MODEL=all-MiniLM-L6-v2
# EMBED_DIMENSION=384

# Default embedding dimension (for OpenAI text-embedding-3-small)
EMBED_DIMENSION=1536
